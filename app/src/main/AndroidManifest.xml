<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mad_cw">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Mobile Application Development - Coursework 01"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MAD_CW">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".IdentifyCarMakeActivity"
            android:label="IDENTIFY CAR MAKE" />
        <activity
            android:name=".HintsActivity"
            android:label="HINTS"/>
        <activity
            android:name=".IdentifyCarImageActivity"
            android:label="IDENTIFY CAR IMAGE" />
        <activity
            android:name=".AdvancedLevelActivity"
            android:label="ADVANCED LEVEL" />
    </application>

</manifest>